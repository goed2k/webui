# 留空则使用当前站点同源路径 /api/v1（开发时走 Vite 代理）
VITE_API_BASE=

# 为 true 时使用内置 Mock，无需真实 daemon
VITE_USE_MOCK=false

# 仅用于 vite.config 中 dev server 代理目标（HTTP + WebSocket）
VITE_DEV_PROXY_TARGET=http://127.0.0.1:18080
